class Solution {
    public boolean isPowerOfFour(int num) {
        
        if (num<=0) return false;
        double res = Math.log(num)/Math.log(4);
        if(res%1==0) 
            return true;
        else 
            return false;
    }
}
